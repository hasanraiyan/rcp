# Your OpenAI API key (required for the LangChain model)
OPENAI_API_KEY=sk-...

# Model to use (optional, defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# RCP manifest URL (optional, defaults to localhost Express example)
RCP_MANIFEST_URL=http://localhost:4321/rcp/manifest
